# 🎯 Trivia Quiz App

A lightweight interactive quiz application built with **HTML**, **CSS**, and **JavaScript**, powered by the **Open Trivia Database API**.

## 🚀 Technologies & Skills
- **JavaScript (ES6+)**
  - Dynamic DOM generation  
  - Timers with `setInterval`  
  - API calls using `fetch()`  
  - Handling user inputs & answer shuffling  
- **OpenTDB API** for categories and questions  
- **HTML/CSS** for structure and UI styling  

## ✨ Main Features
- Fetches categories dynamically when the page loads  
- Customizable quiz (number of questions, category, difficulty, type)  
- 30-second countdown timer for each question  
- Auto-advance when time runs out  
- Displays final score at the end  
- Smooth scrolling between questions  

## ▶️ Run the Project
Open `index.html` in your browser — no backend required.
